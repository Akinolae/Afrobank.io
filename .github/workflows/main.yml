name: deploy

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/setup-node@v2
        with:
          version: "14"
          cache: yarn
          cache-dependency-path: "./Afrobank/yarn.lock"
      - run: yarn install
