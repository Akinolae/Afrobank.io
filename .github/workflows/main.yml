name: deploy

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/setup-node@v2
        with: 
          node_version: "14"
          cache: yarn
      - run:  yarn install
